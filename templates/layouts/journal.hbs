<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{meta.title}}</title>
    <meta name="description" content="{{meta.description}}" />
    <link rel="canonical" href="{{meta.canonicalUrl}}" />
    <link rel="stylesheet" href="{{assets.styles}}" />
    <script defer src="{{assets.scripts}}"></script>
    <style>
      :root {
        {{themeVars}}
      }
    </style>
  </head>
  <body class="ual-body layout-journal">
    <div class="journal-shell">
      <header class="site-header journal-header">
        <a class="logo" href="{{assets.base}}/">{{site.siteTitle}}</a>
        <nav class="site-nav">
          {{#each navigation}}
          <a href="{{href}}" class="nav-link {{#if active}}is-active{{/if}}">{{label}}</a>
          {{/each}}
        </nav>
      </header>
      <main class="site-main journal-main">
        {{#each sections}}
          {{{this}}}
        {{/each}}
      </main>
      <footer class="site-footer journal-footer">
        <div class="footer-links">
          {{#each socialLinks}}
          <a href="{{href}}" target="_blank" rel="noreferrer">{{label}}</a>
          {{/each}}
        </div>
      </footer>
    </div>
  </body>
</html>

